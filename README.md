#Weather app
Openweathermap free key work over http, while chrome allows to get geoposition only over https. So, you need node.js server on localhoost to run this app.
```
$ npm install
$ node server.js
```
